import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'course_project',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
